import math


a,b,c = eval(input("Please enter the coefficents (a ,b, c): "))
discRoot = math.sqrt(b * b-4 *a * c
root1 = (-b + discRoot) / (2 * a)
root2 = (-b - discRoot) / (2 * a)
print ("the solution are:"root1, root2)

